spring:
  application:
    name: auth
  config:
    import: configserver:http://localhost:8888
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,busrefresh,refresh
  endpoint:
    health:
      show-details: never
