spring:
  application:
    name: mypage
  # Config Server 설정
  config:
    import: configserver:http://localhost:8888
  # Spring Cloud Bus 설정
  cloud:
    bus:
      enabled: true
      refresh:
        enabled: true
# Actuator 엔드포인트 설정
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,busrefresh
  endpoint:
    health:
      show-details: never
